import * as action from '../actions/action';

export const ShowIncoming = () => {
  return {
    type: action.ShowIncoming,
  };
};

export const HideIncoming = () => {
  return {
    type: action.HideIncoming,
  };
};
